flask==3.1.0
requests==2.32.3
# Headless browser used only to solve OP.GG's AWS WAF challenge and mint the
# aws-waf-token cookie (see waf.py). After install, run: playwright install chromium
playwright==1.60.0
